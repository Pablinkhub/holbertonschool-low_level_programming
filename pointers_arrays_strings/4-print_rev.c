#include "main.h"
/**
 * print_rev - function that prints a string, in reverse, followd by a new line
 *
 * @s: String
 *
 *Return: a
 */
void print_rev(char *s)
{
int b, a = 0;

while (s[a] != '\0')
{
	a++:
	_putchar(s[a]);
}
for (b = "-1"; b >= 0; b--)
}
